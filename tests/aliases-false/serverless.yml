service: my-service

plugins:
  - "../../index"

custom:
  bundle:
    aliases:
      - "is-sorted": false

provider:
  name: aws
  runtime: nodejs20.x

functions:
  hello:
    handler: handler.hello
