service: my-service

plugins:
  - '../../index'

custom:
  bundle:
    stats: true
    copyFiles:
      - to: './'
        from: 'public/*'
      - to: './'
        from: 'bin/*'

provider:
  name: aws
  runtime: nodejs10.x

functions:
  hello:
    handler: handler.hello
