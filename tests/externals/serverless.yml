service: my-service

plugins:
  localPath: '../../'
  modules:
    - index

custom:
  bundle:
    forceInclude:
      - mysql

provider:
  name: aws
  runtime: nodejs10.x

functions:
  hello:
    handler: handler.hello
