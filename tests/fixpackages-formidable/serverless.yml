service: my-service

plugins:
  - '../../index'

custom:
  bundle:
    fixPackages:
      - "formidable@1.x"

provider:
  name: aws
  runtime: nodejs12.x

functions:
  hello:
    handler: handler.hello
