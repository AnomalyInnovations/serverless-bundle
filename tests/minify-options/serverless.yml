service: my-service

plugins:
  - '../../index'

custom:
  bundle:
    minifyOptions:
      keepNames: true

provider:
  name: aws
  runtime: nodejs20.x

functions:
  hello:
    handler: handler.hello
